
#Netgear R7000(WIFI)
  - platform: netgear
    host: 192.168.1.1
    username: admin
    password: !secret router_password
    new_device_defaults:
      track_new_devices: False
      hide_if_away: True
#GPSLogger(GPS)
  - platform: gpslogger
    password: !secret gpslogger_password